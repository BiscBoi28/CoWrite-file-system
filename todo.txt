~? and ! limiter testing
~stream and then close server  to testing
~backup primary backup and unkown
~can connect  as the same client for some  reason
~exit for users should add
~instead of file locking, sentence locking is there
~!(decided to not do) !in info, write down all access rights also
~print which files have write and read user access
~create then undo kills the user
?~threadpooling of exec
~load balancing of servers
~multiple undo fix
~!(decided not to d) when no backup is there, then it should show the previously active backup as well
~?nm gets segfault/dies out of nowhere after running for a while
~optimized searching algos for files and sentences
~run optimization of everything (except sentences)

shoud we keep getting updates from servers? (if server crashes, then the cache has no way of being updated)
what scripts do they expect us to run for exec? since we can only put scripts which are in a single file only.
is user b1 is writing to a sentence, if b2 undos that sentence, then that sentence is appended to the end
does the ss create a temp of the file it is streaming (when i undo or write while streaming then it still streams correctly.)




fixes not done yet:
a b. I can add at index 2 here
logs fixing
capital letter testing
remove new lines in exec (for i in {1..5}; do echo '$i'; done become for i in {1.
.
5}; do echo '$i'; done)

crash when streaming and client closes